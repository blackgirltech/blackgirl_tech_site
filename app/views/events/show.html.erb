<div class="eventsArea single-event">
  <div class="maxWidth">
    <div class="right">
      <h1><%= @event.name %></h1>
      <p class="eventDetails">
        Location
        <span>
        <%= @event.venue.first.address %>
        </span>
      </p>
      <p class="eventDetails">
        Date &amp; Time
        <span>
        <%= @event.date.strftime('%a %d %b %C') %> / <%= @event.time.strftime('%H:%mpm') %>
        </span>
      </p>
      <div class="price">
        <% if @rsvp.empty? %>
        <%= form_for @event, url: {action: "rsvp"} do |f| %>

            <label for="card-element">
              Card number
            </label>
            <div id="card-element">
              <!-- a Stripe Element will be inserted here. -->
            </div>

            <!-- Used to display form errors -->
            <div id="card-errors" role="alert"></div>

          <% if @event.refundable %>
            <p> We collect a £5 deposit per event which is refunded back to you once your attendance has been confirmed. However, if you would like to donate this deposit to us, to ensure the running of future events, then check the donate box. </p>
            <%= f.fields_for :rsvps do |rsvp| %>
              <%= rsvp.label :donate, "Check here if you'd like to donate your deposit" %>
              <%= rsvp.check_box :donate %>
            <% end %>
          <% end %>
          <div class="priceLeft">
            <p>
              &pound;<%= @event.price %>0
            </p>
            <%= f.submit "RSVP", class: "cta" %>
          </div>
          <div class="priceRight">
            <p>
              Please read our
              <a href="#">Code of Conduct</a>
            </p>
          <!-- Display explanitory text & rsvp button with no form if customer_id exists -->

        <% end %>

        <p> Is this event not quite for you? Why don't you <a href="/events/<%= @event.id %>/volunteer"> volunteer </a> and help others? </p>

      <% else %>
        <p> You are already attending this event </p>
        <a href="/events/<%= @event.id %>/unrsvp" class="cta"> UNRSVP </a>
    <% end %>


        </div>
      </div>
    </div>
    <div class="left">
      <%= image_tag "aboutImg1" %>
      <h2>About <%=@event.name %></h2>
      <p>
        <%= @event.details %>
      </p>
    </div>
  </div>
</div>


  </div>
</div>

<% content_for :script do %>
  <script>
    initialiseStripe('<%=ENV['STRIPE_PUBLISHABLE_KEY']%>', 'edit_event_<%= @event.id %>');
  </script>
<% end %>
