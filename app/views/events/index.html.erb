<div class="pagesHdr" id="eventsHeader">
    <h1>Events</h1>
    <img src="" alt="">
</div>

<div class="eventsArea">
  <div id="eventsTop">
    <div class="maxWidth">
      <h2>Upcoming</h2>
      <% @events.each do |event| %>
        <%= render "partials/event", locals: {event: event} %>
      <% end %>
      </div>
    </div>
  </div>
</div>
